{
  "name": "sound-subgraph",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph codegen && graph build",
    "deploy-studio": "graph deploy --studio web3-music",
    "deploy-hosted": "graph deploy --product hosted-service snowfork/web3-music",
    "deploy": "graph codegen && graph build && graph deploy --studio web3-music"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.28.0",
    "@graphprotocol/graph-ts": "^0.26.0"
  }
}
